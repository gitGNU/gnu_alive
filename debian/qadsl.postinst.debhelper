# Automatically added by dh_installinit
if [ -x "/etc/init.d/qadsl" ]; then
	update-rc.d qadsl defaults >/dev/null
	if [ -x /usr/sbin/invoke-rc.d ]; then
		invoke-rc.d qadsl start
	else
		/etc/init.d/qadsl start
	fi
fi
# End automatically added section
